[pytest]
testpaths = tests
addopts = -v --tb=short
markers =
    unit: pure function tests, no file I/O
    integration: tests that read real CSV/model files
    api: tests that hit the FastAPI TestClient
